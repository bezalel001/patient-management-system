
  # MediFlow Patient Management System

  This is a code bundle for MediFlow Patient Management System. The original project is available at https://www.figma.com/design/IWrq4iARGob9e1DIvF6dSF/MediFlow-Patient-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # patient-management-system
# patient-management-system
